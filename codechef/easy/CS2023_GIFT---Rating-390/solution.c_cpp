#include <bits/stdc++.h>
using namespace std;

int main() {
    int x, n, m;
    cin >> x >> n >> m;
    if (n <= x + m) 
        cout << "Yes" << endl;
    else 
        cout << "No" << endl;
    return 0;
}
